{
  "name": "awslam",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "infra",
    "backend",
    "admin",
    "renderer",
    "tools/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "patch-package",
    "build": "npm run build -w packages/shared && npm run build -w packages/effects && npm run build -w packages/plugins && npm run build -w backend && npm run build -w admin && npm run build -w renderer && npm run build -w tools/mcp-server && npm run build -w infra",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w packages/effects && npm run typecheck -w packages/plugins && npm run typecheck -w backend && npm run typecheck -w admin && npm run typecheck -w renderer && npm run typecheck -w tools/mcp-server && npm run typecheck -w infra",
    "setup": "tsx scripts/setup.ts",
    "setup-config": "tsx scripts/setup-config.ts",
    "manage-domains": "tsx scripts/manage-domains.ts",
    "post-deploy": "npx tsx scripts/post-deploy.ts",
    "post-deploy-staging": "npx tsx scripts/post-deploy-staging.ts",
    "migrate-commerce-private-table": "npx tsx scripts/migrate-commerce-private-table.ts",
    "test:e2e:staging": "STAGING_E2E=1 playwright test review-flow --project=chromium --workers=1"
  },
  "dependencies": {
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.14",
    "tailwindcss": "^4.1.17"
  },
  "devDependencies": {
    "@aws-sdk/client-acm": "^3.980.0",
    "@aws-sdk/client-cloudformation": "^3.980.0",
    "@aws-sdk/client-secrets-manager": "^3.980.0",
    "@aws-sdk/client-ssm": "^3.980.0",
    "@aws-sdk/client-sts": "^3.980.0",
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.10.1",
    "patch-package": "^8.0.1",
    "prompts": "^2.4.2",
    "tsx": "^4.7.1"
  }
}

# AMODX AI Bridge (MCP)

This Node.js server implements the **Model Context Protocol**, allowing local LLMs (Claude Desktop, Cursor) to control the AMODX cloud.

## 🤖 Capabilities

*   **Architecture Awareness:** Knows the schema of Blocks (`get_block_schemas`).
*   **Full Control:** Can create Tenants, Pages, and Context items.
*   **Secure:** Uses the **Master API Key** to bypass Cognito requirements.

## 🛠 Setup

1.  **Environment:** Requires `.env` (Generated by `npm run post-deploy` in root).
2.  **Build:** `npm run build`.
3.  **Install:** `npm run setup`. (Configures `claude_desktop_config.json`).

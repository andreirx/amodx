# INFRA-SPLIT-1 evidence

The **deploy** files here are generated by the **operator** during the staging rehearsal and prod
deploy, per `docs/runbooks/deploy-infra-split-1.md`. `cdk deploy` mutates live infrastructure and is
the human gate; fabricating its output would be a false evidence claim.

Already present (BUILDER-EXECUTED):

- `infra-split-1-synth-counts-staging.md` — real `cdk synth -c stage=staging` resource counts
  (parent 446 < 500; CatalogApi 57; Commerce/Engagement unchanged). Credential-free (the staging
  hosted-zone lookup is cached in `infra/cdk.context.json`), reaches no AWS API.
- `infra-split-1-cdk-diff-staging.md` + `…-staging.raw.txt` — real `cdk diff -c stage=staging
  AmodxStack-staging --no-change-set` (review-6 item 1). **Read-only** (`GetTemplate` + local diff;
  no change set, no mutation). Proves the four v2 gates: 56 catalog resources move out of the parent,
  catalog routes unchanged (integrations retargeted in place), no stateful replacement, no
  Commerce/Engagement structural delta. Surfaces the one non-slice item (`POST /email/dns-check`
  drift). EXECUTED 2026-08-10 against the `UPDATE_ROLLBACK_COMPLETE` staging baseline.

Expected files after the operator runs the runbook (require live credentials + a live mutation):

- `infra-split-1-deploy-staging.txt` — `cdk deploy` transcript, `UPDATE_COMPLETE`, no nested
  `CREATE_FAILED` / duplicate-route-key (review-6 item 2)
- staging route-probe results for all 14 catalog routes + neighbours (review-6 item 3)
- `infra-split-1-cdk-diff-prod.txt`, `infra-split-1-deploy-prod.txt`
